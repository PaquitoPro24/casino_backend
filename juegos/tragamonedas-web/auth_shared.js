// Shared Authentication Logic - Royal Crumbs

// Usar el origen actual del navegador (funciona local y en producción)
const API_URL = window.location.origin;

// No se necesita lógica de tokens o logout aquí
// La autenticación se maneja por cookies de sesión del backend principal
